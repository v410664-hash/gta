name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libffi-dev libssl-dev build-essential
        pip install --upgrade pip setuptools Cython==0.29.33 buildozer

    - name: Accept Android SDK Licenses & Build
      run: |
        yes | buildozer android debug
      env:
        ACCEPT_BUILD_TOOLS_CHECKSUM: "true"

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: schedule-app-apk
        path: bin/*.apk
