name: CI

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          git zip unzip openjdk-17-jdk autoconf automake libtool pkg-config gettext \
          zlib1g-dev libncurses5-dev libncursesw5-dev libffi-dev libssl-dev build-essential

    - name: Install Buildozer and P4A from master
      run: |
        pip install --upgrade pip
        pip install --upgrade "cython<3"
        pip install --upgrade git+https://github.com/kivy/python-for-android.git
        pip install --upgrade git+https://github.com/kivy/buildozer.git

    - name: Build Android APK
      run: |
        yes | buildozer -v android debug

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: bin/*.apk
